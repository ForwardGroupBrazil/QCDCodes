For the combinedSeeder:

0) Make sure the three new files for RecoTracker/TkSeedGenerator are
	copied to RecoTracker/TkSeedGenerator/plugins

1) in HLTrigger/Configuration/python/HLT_XXXX_cff.py rename 
	hltL3TrajectorySeed -> hltL3TrajectorySeedOrig

2) in HLT_XXXX_cff.py change the sequence 
	HLTL3muonTkCandidateSequence = cms.Sequence(
    	HLTDoLocalPixelSequence + HLTDoLocalStripSequence
    	+ cms.SequencePlaceholder("comboSeeds_seq")
    	+ cms.SequencePlaceholder("hltL3TrajectorySeed")
    	+ hltL3TrackCandidateFromL2 )

3) Copy UserCode/L3Switches/python/Switches.py to your working directory
	(the directory with your configuration file)

4) Copy UserCode/L3Switches/python/ptEtaRescale.py to your working
	directory (the directory with your configuration file)

4) add to you configuration file
	from Switches import *

	SwitchToBaseline(process)
	SwitchToOIState(process)
	SwitchToOIHit(process)
	#SwitchToBaselinePP(process)
	
	SwitchToCombo(process)
	process.comboSeeds_seq = cms.Sequence(
	    process.hltTrajSeedIOHit
	    + process.hltTrajSeedOIState
	#    + process.hltTrajSeedOIHit
	    )
	process.hltL3TrajectorySeed.seedCollections = cms.VInputTag(
	    cms.InputTag('hltTrajSeedIOHit',''),
	    cms.InputTag('hltTrajSeedOIState',''),
	#    cms.InputTag('hltTrajSeedOIHit','')
	    )
	
5) in comboSeeds_seq and seedCollections, include whatever combination
	of seeding algorithms you want to use
