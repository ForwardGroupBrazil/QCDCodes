// Put here the modules you want the cfg file to use,
// then include this file in your cfg file.
// i.e. in MuonTrackSelector.cfg replace 'module demo = MuonTrackSelector {} '
// with 'include "producerDir/MuonTrackSelector/data/MuonTrackSelector.cfi" '.
// (Remember that filenames are case sensitive.)

module muonTrackSelector = MuonTrackSelector {
  InputTag muonsTag = muons
  InputTag selectionTag = TMLastStationLoose
}
